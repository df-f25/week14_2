name: dev-cd
on:
  push:
    branches:
      - develop
jobs:
  deploy-job:
    environment: "development-env"
    runs-on: ubuntu-latest 
    steps:
      - name: Welcome Message
        run: echo "Welcome to CD Pipeline for Development Branch"
      # - name: Clone Repo On the Runner
      #   uses: actions/checkout@v6
      # - name: Login To Docker Hub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DHUB_USER }}
      #     password: ${{ secrets.DHUB_TOKEN }}
      # - name: Build Image
      #   run: docker image build -t ${{secrets.DHUB_USER}}/w14:latest .
      # - name: Push Image On Docker Hub
      #   run: docker push ${{secrets.DHUB_USER}}/w14:latest
      - name: Ask Render.com To Peform the New Deployment
        run: curl -X POST ${{secrets.RENDER_WEBHOOK_DEVELOP}}
      